apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
patches:
- path: replica_count.yaml
# Your overlay customizations
namePrefix: prod-
labels:
- includeSelectors: true
  pairs:
    environment: production
